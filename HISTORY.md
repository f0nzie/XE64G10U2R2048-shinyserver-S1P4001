


```
    1  R
    2  sudo R
    3  sudo apt install libudunits2-dev -y
    4  sudo Rscript -e 'install.packages("sf")'
    5  sudo apt install gdal -y
    6  sudo apt install rgdal -y
    7  sudo add-apt-repository ppa:ubuntugis/ppa
    8  sudo add-apt-repository ppa:ubuntugis/ppa -y
    9  sudo apt update
   10  sudo apt-get install gdal-bin
   11  sudo apt-get install gdal-bin -y
   12  sudo Rscript -e 'install.packages("sf")'
   13  sudo apt-get install libgdal-dev
   14  sudo apt install libmysqlclient-dev
   15  sudo apt install libmysqlclient-dev -y
   16  sudo Rscript -e 'install.packages("sf")'
   17  sudo dpkg -i libssl1.0.0.deb
   18  sudo wget -O libssl1.0.0.deb http://ftp.debian.org/debian/pool/main/o/openssl/libssl1.0.0_1.0.1t-1+deb8u8_amd64.deb
   19  sudo dpkg -i libssl1.0.0.deb
   20  sudo dpkg -i libssl1.0.0.deb -y
   21  sudo yes|dpkg -i libssl1.0.0.deb -y
   22  yes|sudo dpkg -i libssl1.0.0.deb -y
   23  yes|sudo dpkg -i libssl1.0.0.deb
   24  sudo yes|dpkg -i libssl1.0.0.deb



```